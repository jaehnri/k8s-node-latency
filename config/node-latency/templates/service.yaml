apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-latency.fullname" . }}
  labels:
    {{- include "node-latency.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-latency.selectorLabels" . | nindent 4 }}
