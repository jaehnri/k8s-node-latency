apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-latency.fullname" . }}-test-connection"
  labels:
    {{- include "node-latency.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-latency.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
